# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Emoji Detector' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Emoji Detector
	pod 'Firebase/Core'

end

target 'Emoji Detector MessagesExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Emoji Detector MessagesExtension
	pod 'Firebase/Core'
	pod 'Firebase/MLVision'
	pod 'Firebase/MLVisionFaceModel'

end
